# 4.24 notes

## 蓝书，cf和at的一些题目

+ ABC441F，**前后缀分解技巧**，预处理pre和suf信息，可以计算**除去某个/段元素后的最优值，或关于不相交的信息的最优值**，如除$a[i]$的gcd最大值，选出两段不相交连续子数组的和最大，除去某个子树的最值。
+ P3572，**单调队列优化dp**，每个状态由前面k个固定长度的状态转移，考虑怎么维护最优性，两个维度
+ UVA1330，最大子矩形，最大1矩形这种问题