# 5.2 notes

+ ABC456E，转化成判断**有向图是否存在环**，跑反向图上的拓扑排序，判断每个SCC是否有大于1的，也可以
+ ABC456F，线性dp，分析可知，最优一定是k或k+1长，对于每个k和k+1长的段dp。dp[i]表示在段中以第i天结尾的最小，暴力是O(NK)，转移方程可发现只依赖前两项，只涉及加法和求最小值，可以利用矩阵加速转移，线段树维护区间矩阵乘积，优化为O(NlogN)。用**矩阵优化的线性dp**特征是**只依赖于前面较少有限状态，且可以线性转移**
+ ABC456G，计数，归结为求01串中连续1的长度不超过k的方案数，利用生成函数计数；或者容斥原理，

