# 4.15 notes

## 字符串基础一些小总结

### 

## 扫描线

+ 线段树的一个特殊用法，不知道能不能联系起来
+ 求矩形面积并，离散化，然后从下往上扫描，扫到矩形的下底边，区间加1，扫到上边，区间减1，想要的信息是整个区间上不为0的个数。无法维护
+ 给每个区间标记**当前区间被完全覆盖的次数**cnt，维护区间内非0的长度len
+ 区间+1，-1时，若全包含，则修改标签cnt，然后更新len：如果cnt>=1，len为区间长度，否则为两个儿子的len和（叶子节点是0）。
+ 不好维护是由于**减法操作具有“破坏性”且难以回溯状态**

## 生成函数

